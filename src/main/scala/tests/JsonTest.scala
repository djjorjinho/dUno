package dUno.tests

import net.liftweb.json._
import net.liftweb.json.JsonAST._
import net.liftweb.json.JsonDSL._

object JsonTest {
  def main(args : Array[String]) : Unit = {
		 val x = List(Map("A" -> 1, "B" -> 2))
		 
		 
		 true 
  }
}
